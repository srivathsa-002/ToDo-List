import React from 'react';
import PlaylistAddCheckIcon from '@material-ui/icons/PlaylistAddCheck';

export default class CompleteIcon extends React.Component {
    render() {
        return (<PlaylistAddCheckIcon data-testid="complete"/>);
    }
}