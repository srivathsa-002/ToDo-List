import React from 'react';
import ListIcon from '@material-ui/icons/List';

export default class AllIcon extends React.Component {
    render() {
        return (<ListIcon data-testid="all"/>);
    }
}